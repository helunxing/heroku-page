{
  "name": "node-js-getting-started",
  "version": "0.3.0",
  "description": "A sample Node.js app using Express 4",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build": "npm --prefix server run build",
    "start": "npm run build && node server/server.js",
    "dev_start": "node server/server.js"
  },
  "license": "MIT"
}
