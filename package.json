{
  "name": "node-js-getting-started",
  "version": "0.3.0",
  "description": "A sample Node.js app using Express 4",
  "engines": {
    "node": "16.x"
  },
  "main": "index.js",
  "scripts": {
    "start": "node server/app.js",
    "local build & start": "cd client && npm install && npm run build && node ../server/app.js",
    "test": "node test.js",
    "build": "cd client && npm install && npm run build"
  },
  "license": "MIT"
}
